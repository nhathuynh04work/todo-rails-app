<div class="min-h-screen bg-gray-950 text-gray-300 font-sans">
  <nav class="border-b border-gray-800 bg-gray-950 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4">
      <div class="flex h-16 justify-between items-center">
        <div class="flex items-center">
          <span class="text-xl font-bold text-white tracking-tight">
            Task<span class="text-indigo-500">Flow</span>
          </span>
        </div>
        <div class="flex items-center gap-6">
          <div class="flex flex-col items-end">
            <span class="text-xs text-gray-500 uppercase tracking-wider font-semibold">Signed in as</span>
            <span class="text-sm text-gray-300 font-medium">
              <%= current_user.email %>
            </span>
          </div>
          <div class="pl-6 border-l border-gray-800">
            <%= button_to "Sign out", logout_path, method: :delete, 
                class: "text-sm font-medium text-red-400 hover:text-red-300 transition-colors cursor-pointer" %>
          </div>
        </div>
      </div>
    </div>
  </nav>
  <main class="max-w-7xl mx-auto px-4 py-8">
    <div class="text-center py-20 border border-dashed border-gray-800 rounded-xl">
      <h1 class="text-3xl font-bold text-white mb-4">Welcome back!</h1>
      <p class="text-gray-500">Your tasks will load here soon.</p>
    </div>
  </main>
</div>